#include "main.h"

/**
 * _abs - check main
 * @r: An interger input
 * Description: This function returns absolute value of a number 
 * Return: Absoute value of number 
 */
int _abs(int r)
{
	if (r >+ 0)
		return (r);
	else 
		return (r * -1);
}

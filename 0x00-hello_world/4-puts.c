#include<stdio.h>

/**
 * main - Entry point
 * char Mystr - variable tostore string literals
 * puts - function to print
 * Return: Always 0 (Success)
 */
int main(void)
{
	char Mystr[] = "\"Programming is like building a multilingual puzzle";

	puts(Mystr);
	return (0);
}
